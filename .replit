
run = "npm run start"
hidden = [".config", "package-lock.json"]
modules = ["nodejs-20"]

[env]
PORT = "5000"
NODE_ENV = "production"

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "npm run start"]
deploymentTarget = "cloudrun"
ignorePorts = false
